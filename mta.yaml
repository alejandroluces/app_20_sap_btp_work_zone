_schema-version: "3.2"
ID: webina-mta
version: 0.0.1
modules:
- name: webina-mta-approuter
  type: approuter.nodejs
  path: webina-mta-approuter
  requires:
  - name: webina-mta_html_repo_runtime
  - name: uaa_webina-mta
  - name: webina-mta-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: webina-mta_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: webina-mta_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sapbtpapp20.zip
      name: sapbtpapp20
      target-path: resources/
    - artifacts:
      - sapbtpapp21.zip
      name: sapbtpapp21
      target-path: resources/
- name: sapbtpapp20
  type: html5
  path: app20
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: sapbtpapp21
  type: html5
  path: app21
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: webina-mta_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_webina-mta
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: webina-mta-xsuaa-service
    service-plan: application
- name: webina-mta_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: webina-mta-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: webina-mta-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
